(()=>{'use strict';if(new URLSearchParams(location.search).get('customer')==='1')return;const URL='https://dmhlfrnjvjgsooamioak.supabase.co',KEY='sb_publishable_HvY0X0eDKCZ8dMKsLAZICg_6MBsmMUi',db=()=>window.supabase.createClient(URL,KEY);function inject(){const c=document.getElementById('content');if(!c||document.getElementById('finalClosePanel'))return;if(!/Day Closing/i.test(c.textContent||''))return;const p=document.createElement('div');p.id='finalClosePanel';p.className='panel';p.style='margin-top:20px;border:2px solid #111;padding:22px';p.innerHTML='<h2>Final Day Settlement</h2><p class="muted">This is the official cloud closing. It carries actual cash to the next day and resets daily figures.</p><label>Actual Cash in Hand (NPR)</label><input id="finalActualCash" class="input" type="number" min="0" step="1" placeholder="Enter counted cash"><label style="display:block;margin-top:10px">Closing Notes (optional)</label><input id="finalCloseNotes" class="input" placeholder="Notes"><button id="finalCloseBtn" class="btn primary full" style="margin-top:14px">Close Business Day</button><div id="finalCloseMsg" class="muted" style="margin-top:10px"></div>';c.appendChild(p);document.getElementById('finalCloseBtn').onclick=closeDay}
async function closeDay(){const v=Number(document.getElementById('finalActualCash')?.value);const msg=document.getElementById('finalCloseMsg');if(!Number.isFinite(v)||v<0){msg.textContent='Enter a valid cash amount.';return}const b=document.getElementById('finalCloseBtn');b.disabled=true;b.textContent='Closing…';const {data,error}=await db().rpc('close_business_day',{p_actual_cash:v,p_notes:document.getElementById('finalCloseNotes')?.value.trim()||null});if(error){b.disabled=false;b.textContent='Close Business Day';msg.textContent=error.message;return}msg.innerHTML='Closed. Expected cash: NPR '+Number(data.expected_cash||0).toFixed(2)+' • Actual: NPR '+Number(data.actual_cash||0).toFixed(2)+' • Variance: NPR '+Number(data.variance||0).toFixed(2);setTimeout(async()=>{await db().auth.signOut();location.reload()},900)}const o=new MutationObserver(inject);o.observe(document.documentElement,{childList:true,subtree:true});setInterval(inject,1000);})();

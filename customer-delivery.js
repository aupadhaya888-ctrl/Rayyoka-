(()=>{'use strict';const customerMode=new URLSearchParams(location.search).get('customer')==='1';if(!customerMode)return;const URL='https://dmhlfrnjvjgsooamioak.supabase.co',KEY='sb_publishable_HvY0X0eDKCZ8dMKsLAZICg_6MBsmMUi',db=window.supabase?.createClient(URL,KEY),RYKEY='rayyoka_ry_customer';const $=id=>document.getElementById(id),money=n=>'NPR '+Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:0,maximumFractionDigits:2});let cart={},registered=null;try{registered=JSON.parse(localStorage.getItem(RYKEY)||'null')}catch(e){}
function showRegistration(){if($('ryGate')||!$('placeOrder'))return;const d=document.createElement('div');d.id='ryGate';d.style='position:fixed;inset:0;background:rgba(0,0,0,.82);z-index:99999;display:flex;align-items:center;justify-content:center;padding:18px';d.innerHTML='<div style="background:#fff;border-radius:20px;padding:28px;max-width:440px;width:100%;box-shadow:0 20px 60px rgba(0,0,0,.35)"><div style="text-align:center"><div style="font-size:42px;font-weight:800">RY</div><h2>Register your RY ID</h2><p class="muted">Register once before using RAYYOKA online ordering.</p></div><label>Full Name</label><input id="ryName" class="input" placeholder="Your full name"><label>Phone</label><input id="ryPhone" class="input" placeholder="98XXXXXXXX"><label>Email (optional)</label><input id="ryEmail" class="input" type="email" placeholder="you@example.com"><button id="ryRegister" class="btn primary full" style="margin-top:14px">Create RY ID</button><p id="ryErr" class="error" style="display:none"></p></div>';document.body.appendChild(d);$('ryRegister').onclick=register}
async function register(){const name=$('ryName').value.trim(),phone=$('ryPhone').value.trim(),email=$('ryEmail').value.trim();if(!name||!phone){$('ryErr').textContent='Name and phone are required';$('ryErr').style.display='block';return}const btn=$('ryRegister');btn.disabled=true;btn.textContent='Registering…';const {data,error}=await db.rpc('register_ry_customer',{p_full_name:name,p_phone:phone,p_email:email||null});if(error){btn.disabled=false;btn.textContent='Create RY ID';$('ryErr').textContent=error.message;$('ryErr').style.display='block';return}registered={ry_id:data.ry_id,name:data.name,phone:data.phone,email:email};localStorage.setItem(RYKEY,JSON.stringify(registered));$('ryGate').remove();showRegistered()}
function showRegistered(){if(!$('placeOrder')||$('ryWelcome'))return;const x=registered||{};const p=document.createElement('div');p.id='ryWelcome';p.className='panel';p.style='margin-bottom:18px';p.innerHTML='<b>RY ID: '+String(x.ry_id||'')+'</b><small style="display:block;margin-top:4px">Registered customer • '+String(x.name||'')+'</small>';$('placeOrder').closest('.cart-panel')?.prepend(p)}
function trackFood(e){const b=e.target.closest('#cfoods .food');if(!b)return;const id=b.dataset.id;cart[id]=(cart[id]||0)+1}function trackQty(e){const b=e.target.closest('#ccart .qty button');if(!b)return;const id=b.dataset.id,d=Number(b.dataset.d||0);cart[id]=(cart[id]||0)+d;if(cart[id]<=0)delete cart[id]}
function ensureUI(){if(!$('placeOrder'))return false;const type=$('ctype');if(type&&!type.dataset.deliveryBound){type.dataset.deliveryBound='1';type.addEventListener('change',refresh)}if(!$('placeOrder').dataset.finalOrder){$('placeOrder').dataset.finalOrder='1';$('placeOrder').onclick=placeOrder}if(!registered)try{registered=JSON.parse(localStorage.getItem(RYKEY)||'null')}catch(e){}showRegistered();return true}
function refresh(){if(!ensureUI())return;let row=$('cdelivery');if(!row){row=document.createElement('div');row.id='cdelivery';row.innerHTML='<span>Delivery Charge</span><b id="cdeliveryValue">To be confirmed by RAYYOKA staff</b>';const bill=$('ctot')?.parentNode?.parentNode;if(bill)bill.insertBefore(row,bill.lastElementChild)}if($('ctot')){const sub=Number(($('csub')?.textContent||'0').replace(/[^0-9.-]/g,''))||0,svc=Number(($('csvc')?.textContent||'0').replace(/[^0-9.-]/g,''))||0,vat=Number(($('ctax')?.textContent||'0').replace(/[^0-9.-]/g,''))||0;$('ctot').textContent=money(sub+svc+vat)}}
async function placeOrder(){if(!registered)return showRegistration();if(!ensureUI())return;if(!Object.keys(cart).length)return alert('Add at least one item');const name=$('cname')?.value.trim()||registered.name||'',phone=$('cphone')?.value.trim()||registered.phone||'',type=$('ctype')?.value||'Pickup',address=$('caddress')?.value.trim()||'';if(!name)return alert('Enter your name');if(type==='Delivery'&&!address)return alert('Enter delivery address');const items=Object.entries(cart).map(([menu_item_id,quantity])=>({menu_item_id,quantity}));const btn=$('placeOrder');btn.disabled=true;btn.textContent='Placing Order…';const {data,error}=await db.rpc('place_public_order',{p_customer_name:name,p_phone:phone,p_order_type:type,p_address:address,p_items:items,p_delivery_charge:0,p_ry_customer_id:registered.ry_id});if(error){btn.disabled=false;btn.textContent='Place Order';return alert('Order failed: '+error.message)}cart={};const modal=$('modalBox');if(modal){modal.innerHTML='<div class="success">✓</div><h2>Order Received</h2><p>RY ID: <b>'+registered.ry_id+'</b></p><p>Order: <b>'+String(data.order_number)+'</b></p><p>Total before delivery charge: <b>'+money(data.total)+'</b></p><p class="muted">Delivery charge will be confirmed by RAYYOKA staff according to distance.</p><button class="btn primary full" onclick="document.getElementById(\'modal\').classList.remove(\'show\');location.reload()">Done</button>';document.getElementById('modal')?.classList.add('show')}else alert('Order received: '+data.order_number);btn.disabled=false;btn.textContent='Place Order'}
const observer=new MutationObserver(()=>{if(ensureUI())refresh();if(!registered&&$('placeOrder')&&!$('ryGate'))showRegistration()});observer.observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',trackFood,true);document.addEventListener('click',trackQty,true);setTimeout(()=>{if(!registered)showRegistration()},1200);
})();
